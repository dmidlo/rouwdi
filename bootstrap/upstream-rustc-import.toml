[ledger]
schema_version = 1
source_tree = "third_party/rust"
pinned_revision = "800892799d7666fe1dc17abd862100a6cf273718"
adapter_crate = "crates/rouwdi-rustc-upstream"
stage0_rustc = "rustc 1.95.0 (59807616e 2026-04-14), host x86_64-pc-windows-msvc"

[[root_blockers]]
id = "rustc_index"
component = "rustc_index"
source_path = "third_party/rust/compiler/rustc_index"
status = "cleared_by_bootstrap_stage1"
blocker_kind = "raw_cargo_not_bootstrap"
summary = "Raw cargo probes reach rustc_index and fail on the unstable new_range_api surface, but the pinned Rust tree's bootstrap-owned stage1 check compiles rustc_index. The blocker was the probe path, not a source blocker."
primary_probe_command = "cargo run -p rouwdi-rustc-upstream --bin upstream-rustc-probe -- --mode all rustc_index"
generated_by_probe = "crates/rouwdi-rustc-upstream/src/bin/upstream-rustc-probe.rs"
required_probe_mode = "xpy-stage1"
stage0_rustc = "rustc 1.95.0 (59807616e 2026-04-14), host x86_64-pc-windows-msvc"
required_env = []
required_cfg = []
observed_unstable_features = ["new_range_api"]
blocked_components = ["rustc_parse", "rustc_expand", "rustc_resolve", "rustc_hir_analysis", "rustc_borrowck", "rustc_middle", "rustc_mir_build"]

[[root_blockers.probe_attempts]]
name = "raw-rustc-index"
command = "$env:RUSTC_BOOTSTRAP='1'; cargo check --manifest-path third_party/rust/compiler/rustc_index/Cargo.toml -p rustc_index"
exit_code = 101
outcome = "failed"
classification = "raw_cargo_stage0_feature_gate_mismatch"
evidence = "E0658: use of unstable library feature new_range_api in compiler/rustc_index/src/idx.rs"

[[root_blockers.probe_attempts]]
name = "bootstrap-cfg-rustc-index"
command = "$env:RUSTC_BOOTSTRAP='1'; $env:RUSTFLAGS='--cfg=bootstrap'; cargo check --manifest-path third_party/rust/compiler/rustc_index/Cargo.toml -p rustc_index"
exit_code = 101
outcome = "failed"
classification = "raw_cargo_stage0_feature_gate_mismatch"
evidence = "--cfg=bootstrap does not make raw Cargo equivalent to x.py; rustc_index still fails on new_range_api"

[[root_blockers.probe_attempts]]
name = "no-default-features-rustc-index"
command = "$env:RUSTC_BOOTSTRAP='1'; cargo check --manifest-path third_party/rust/compiler/rustc_index/Cargo.toml -p rustc_index --no-default-features"
exit_code = 0
outcome = "passed"
classification = "compiled"
evidence = "rustc_index compiles when its default/nightly feature surface is disabled, isolating the failing raw-Cargo surface"

[[root_blockers.probe_attempts]]
name = "injected-new-range-api-rustc-index"
command = "$env:RUSTC_BOOTSTRAP='1'; $env:RUSTFLAGS='-Zcrate-attr=feature(new_range_api)'; cargo check --manifest-path third_party/rust/compiler/rustc_index/Cargo.toml -p rustc_index"
exit_code = 0
outcome = "passed"
classification = "compiled"
evidence = "Injecting feature(new_range_api) makes rustc_index compile under raw Cargo, proving the exact feature gate requirement"

[[root_blockers.probe_attempts]]
name = "xpy-stage0-rustc-index"
command = "python x.py check compiler/rustc_index --stage 0 -v"
exit_code = 1
outcome = "failed"
classification = "xpy_stage0_check_not_supported"
evidence = "bootstrap rejects stage0 compiler crate checks unless build.local-rebuild=true is configured"

[[root_blockers.probe_attempts]]
name = "xpy-stage1-rustc-index"
command = "python x.py check compiler/rustc_index --stage 1 -v"
exit_code = 0
outcome = "passed"
classification = "compiled"
evidence = "Checking stage1 compiler artifacts{rustc_index}; Build completed successfully"

[[root_blockers.probe_attempts]]
name = "retry-rustc-parse-xpy-stage1"
command = "python x.py check compiler/rustc_parse --stage 1 -v"
exit_code = 0
outcome = "passed"
classification = "compiled"
evidence = "Checking stage1 compiler artifacts{rustc_parse}; Build completed successfully"

[[root_blockers.probe_attempts]]
name = "retry-rustc-middle-xpy-stage1"
command = "python x.py check compiler/rustc_middle --stage 1 -v"
exit_code = 0
outcome = "passed"
classification = "compiled"
evidence = "Checking stage1 compiler artifacts{rustc_middle}; Build completed successfully"

[[root_blockers.probe_attempts]]
name = "retry-rustc-mir-build-xpy-stage1"
command = "python x.py check compiler/rustc_mir_build --stage 1 -v"
exit_code = 0
outcome = "passed"
classification = "compiled"
evidence = "Checking stage1 compiler artifacts{rustc_mir_build}; Build completed successfully"

[[bootstrap_adapter_probes]]
name = "mir_handoff_payload_adapter"
adapter_crate = "third_party/rust/src/tools/rouwdi-mir-adapter-probe"
source_path = "third_party/rust/src/tools/rouwdi-mir-adapter-probe/src/lib.rs"
command = "python x.py check src/tools/rouwdi-mir-adapter-probe --stage 1 -v"
workdir = "third_party/rust"
stage = 1
exit_code = 0
outcome = "passed"
classification = "bootstrap_adapter_typechecked"
evidence = "Checking stage1 rouwdi-mir-adapter-probe (stage0 -> stage1, x86_64-pc-windows-msvc); Finished release profile for rouwdi-mir-adapter-probe; Build completed successfully in 0:00:21"
authoritative = true
normal_workspace_probe_command = "cargo check -p rouwdi-rustc-upstream --features real-rustc-mir-payload"
payload_state = "payload_context_attempted"
payload_carrier_created = true
artifact_locate_command = "cargo run -p rouwdi-rustc-upstream --bin mir-payload-carrier -- --json"
artifact_build_command = "cargo run -p rouwdi-rustc-upstream --bin direct-rustc-private-pack-builder"
export_manifest_path = "bootstrap/mir-payload-export-manifest.toml"
payload_loaded_into_rouwdi_facade = false
payload_load_blocker_kind = "none"
payload_load_blocker_reason = "The direct rustc-private pack builder built the listed closure for wasm32-wasip1 without invoking the stage2 wasm-host LLVM route, then emitted a loadable wasm32-wasip1 rouwdi_mir_adapter_probe module with ABI v1 exports. The embedded-registry loader instantiated that module, called version/stage/descriptor/valid-input/execute, and the payload emitted real TyCtxt::optimized_mir body identity/hash before recording rustc_middle::ty::TyCtxt::collect_and_partition_mono_items output. No fabricated HIR, TyCtxt, Providers, Body<'tcx>, MIR, or mono items is accepted."
payload_bundle_format = "rouwdi_compiler_payload_bundle.v1"
payload_loader_status = "loadable"
payload_next_required_artifact_format = "codegen_handoff"
next_artifact_command = "Advance from the emitted MIR body proof to the upstream rustc_middle::ty::TyCtxt::collect_and_partition_mono_items query without fabricating mono items."
next_artifact_command_exit_code = 0
next_artifact_command_evidence = "Current direct-pack command exits 0 and emits .rouwdi/direct-rustc-private-pack/target/wasm32-wasip1/release/rouwdi_mir_adapter_probe.wasm (sha256 b9ae49950e1f1f12768211d4b5f8fa9f6a8ebb52cacafe2bb701688db59f7c54, 88495302 bytes). The embedded-registry loader executes that module and records mono_items_collected with TyCtxt::optimized_mir output plus rustc_middle::ty::TyCtxt::collect_and_partition_mono_items output. The next artifact change is entering collect_and_partition_mono_items without fabricating mono items."
typechecked_entrypoints = [
  "rouwdi_mir_adapter_probe::mir_handoff_payload_adapter<'a, 'tcx>(&rustc_session::Session, rustc_middle::ty::TyCtxt<'tcx>, &rustc_middle::query::Providers, rustc_hir::def_id::LocalDefId) -> BootstrapMirHandoffPayload<'a, 'tcx>",
  "rouwdi_mir_adapter_probe::register_mir_build_providers(&mut rustc_middle::util::Providers)",
]
upstream_type_surface = [
  "rustc_middle::mir::Body<'tcx>",
  "rustc_middle::ty::TyCtxt<'tcx>",
  "rustc_middle::query::Providers",
  "rustc_middle::util::Providers",
  "rustc_session::Session",
  "rustc_hir::def_id::LocalDefId",
  "rustc_span::def_id::LocalDefId",
  "rustc_mir_build::provide",
  "rustc_middle::hooks::Providers::build_mir_inner_impl",
]
provider_surface = [
  "rustc_mir_build::provide(&mut rustc_middle::util::Providers)",
  "rustc_middle::util::Providers::queries",
  "rustc_middle::util::Providers::hooks",
  "rustc_middle::hooks::Providers::build_mir_inner_impl",
]
required_upstream_crates = [
  "rustc_builtin_macros",
  "rustc_expand",
  "rustc_resolve",
  "rustc_interface",
  "rustc_hir_analysis",
  "rustc_lint",
  "rustc_middle",
  "rustc_session",
  "rustc_hir",
  "rustc_span",
  "rustc_mir_build",
  "rustc_parse",
  "rustc_passes",
  "rustc_query_impl",
]
required_upstream_modules = [
  "rustc_middle::mir",
  "rustc_middle::ty",
  "rustc_middle::query",
  "rustc_middle::util",
  "rustc_middle::hooks",
  "rustc_interface",
  "rustc_expand",
  "rustc_resolve",
  "rustc_hir_analysis",
  "rustc_session",
  "rustc_hir::def_id",
  "rustc_span::def_id",
  "rustc_mir_build",
]

[bootstrap_adapter_probes.artifact]
crate_name = "rouwdi_mir_adapter_probe"
artifact_kind = "wasm_module"
artifact_format = "wasm_module"
path = ".rouwdi/direct-rustc-private-pack/target/wasm32-wasip1/release/rouwdi_mir_adapter_probe.wasm"
sha256 = "b9ae49950e1f1f12768211d4b5f8fa9f6a8ebb52cacafe2bb701688db59f7c54"
size_bytes = 88495302
host_triple = "wasm32-wasip1"
profile = "release"
emitted_by = "cargo run -p rouwdi-rustc-upstream --bin direct-rustc-private-pack-builder"
loadable_by_rouwdi_wasm = true

[bootstrap_adapter_probes.metadata_artifact]
crate_name = "rouwdi_mir_adapter_probe"
artifact_kind = "rustc_metadata"
artifact_format = "rmeta"
path = ".rouwdi/direct-rustc-private-pack/target/wasm32-wasip1/release/deps/librouwdi_mir_adapter_probe-444a985972f2f985.rmeta"
sha256 = "3df87cffb6d91e9b84993ebb85298c3467491ccf28523861338afa4178ecf952"
size_bytes = 213323
host_triple = "x86_64-pc-windows-msvc"
profile = "release"
emitted_by = "python x.py check src/tools/rouwdi-mir-adapter-probe --stage 1 -v"
loadable_by_rouwdi_wasm = false

[[components]]
name = "rustc_lexer"
source_path = "third_party/rust/compiler/rustc_lexer"
desired_role = "canonical Rust tokenization already used by the existing frontend ratchet"
import_status = "upstream_backed"
attempted = true
adapter_path = "crates/rouwdi-rustc"
probe_command = "cargo check -p rouwdi-rustc"
blocker_kind = "none"
exact_blocker = ""

[[components]]
name = "rustc_error_codes"
source_path = "third_party/rust/compiler/rustc_error_codes"
desired_role = "upstream compiler diagnostic code table imported as the first adapter payload beyond lexer/parser"
import_status = "imported"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "cargo check -p rouwdi-rustc-upstream"
blocker_kind = "none"
exact_blocker = ""

[[components]]
name = "rustc_index"
source_path = "third_party/rust/compiler/rustc_index"
desired_role = "upstream index vector and interval table infrastructure shared by rustc_parse, rustc_middle, and MIR import surfaces"
import_status = "adapter_embedded"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "cargo test -p rouwdi-rustc-upstream rustc_index_adapter_surface_exercises_real_upstream_indexvec -- --exact"
blocker_kind = "none"
exact_blocker = ""
adapter_symbol = "rouwdi_rustc_upstream::rustc_index_adapter_surface"
adapter_evidence = "The adapter crate depends on third_party/rust/compiler/rustc_index with default nightly features disabled, then rouwdi_rustc_upstream::rustc_index_adapter_surface constructs rustc_index::IndexVec<usize, &str>, calls rustc_index::Idx::new/index, indexes through the upstream IndexSlice surface, and is covered by the exact test command above."

[[components]]
name = "rust_analyzer_parser"
source_path = "third_party/rust/src/tools/rust-analyzer/crates/parser"
desired_role = "temporary upstream-adjacent parser scaffolding used only until rustc_parse is imported"
import_status = "temporary_scaffolding"
attempted = true
adapter_path = "crates/rouwdi-rustc"
probe_command = "cargo check -p rouwdi-rustc"
blocker_kind = "not_canonical_rustc_parse"
exact_blocker = "The current parser is from rust-analyzer, not rustc_parse. It remains only to preserve the existing parse ratchet until rustc_parse is imported."

[[components]]
name = "rustc_parse"
source_path = "third_party/rust/compiler/rustc_parse"
desired_role = "canonical rustc parser and AST construction, replacing the current rust-analyzer parser scaffolding"
import_status = "bootstrap_probe_passed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "cargo run -p rouwdi-rustc-upstream --bin upstream-rustc-probe -- --require-compiled --mode xpy-stage1 rustc_parse"
blocker_kind = "adapter_not_embedded"
exact_blocker = "The pinned Rust bootstrap stage1 probe compiles rustc_parse after the rustc_index root analysis, but rouwdi-rustc-upstream has not embedded or exposed rustc_parse through the adapter API yet."
immediate_dependency_blocker = "rustc_index"
shared_blocker = "rustc_index"

[[components]]
name = "rustc_expand"
source_path = "third_party/rust/compiler/rustc_expand"
desired_role = "canonical macro expansion and builtin macro integration"
import_status = "bootstrap_probe_passed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "cargo run -p rouwdi-rustc-upstream --bin upstream-rustc-probe -- --require-compiled --mode xpy-stage1 rustc_expand"
blocker_kind = "adapter_not_embedded"
exact_blocker = "The pinned Rust bootstrap stage1 probe compiles rustc_expand after the rustc_index root analysis, but rouwdi-rustc-upstream has not embedded or exposed rustc_expand through the adapter API yet."
immediate_dependency_blocker = "rustc_index"
shared_blocker = "rustc_index"

[[components]]
name = "rustc_resolve"
source_path = "third_party/rust/compiler/rustc_resolve"
desired_role = "canonical rustc name resolution"
import_status = "bootstrap_probe_passed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "cargo run -p rouwdi-rustc-upstream --bin upstream-rustc-probe -- --require-compiled --mode xpy-stage1 rustc_resolve"
blocker_kind = "adapter_not_embedded"
exact_blocker = "The pinned Rust bootstrap stage1 probe compiles rustc_resolve after the rustc_index root analysis, but rouwdi-rustc-upstream has not embedded or exposed rustc_resolve through the adapter API yet."
immediate_dependency_blocker = "rustc_index"
shared_blocker = "rustc_index"

[[components]]
name = "rustc_hir_analysis"
source_path = "third_party/rust/compiler/rustc_hir_analysis"
desired_role = "canonical type checking, well-formedness, and coherence analysis"
import_status = "bootstrap_probe_passed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "cargo run -p rouwdi-rustc-upstream --bin upstream-rustc-probe -- --require-compiled --mode xpy-stage1 rustc_hir_analysis"
blocker_kind = "adapter_not_embedded"
exact_blocker = "The pinned Rust bootstrap stage1 probe compiles rustc_hir_analysis after the rustc_index root analysis, but rouwdi-rustc-upstream has not embedded or exposed rustc_hir_analysis through the adapter API yet."
immediate_dependency_blocker = "rustc_index"
shared_blocker = "rustc_index"

[[components]]
name = "rustc_borrowck"
source_path = "third_party/rust/compiler/rustc_borrowck"
desired_role = "canonical MIR borrow checking"
import_status = "bootstrap_probe_passed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "cargo run -p rouwdi-rustc-upstream --bin upstream-rustc-probe -- --require-compiled --mode xpy-stage1 rustc_borrowck"
blocker_kind = "adapter_not_embedded"
exact_blocker = "The pinned Rust bootstrap stage1 probe compiles rustc_borrowck after the rustc_index root analysis, but rouwdi-rustc-upstream has not embedded or exposed rustc_borrowck through the adapter API yet."
immediate_dependency_blocker = "rustc_index"
shared_blocker = "rustc_index"

[[components]]
name = "rustc_middle"
source_path = "third_party/rust/compiler/rustc_middle"
desired_role = "MIR data structures, ty context, query model, and compiler metadata needed by the MIR handoff"
import_status = "adapter_partially_embedded"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "python x.py check src/tools/rouwdi-mir-adapter-probe --stage 1 -v"
blocker_kind = "none"
exact_blocker = "The bootstrap-owned rouwdi-mir-adapter-probe crate type-checks under the pinned Rust tree's x.py stage1 ToolRustcPrivate path and references rustc_middle::mir::Body<'tcx>, rustc_middle::ty::TyCtxt<'tcx>, rustc_middle::query::Providers, rustc_middle::util::Providers, rustc_session::Session, rustc_hir::def_id::LocalDefId, rustc_span::def_id::LocalDefId, rustc_mir_build::provide, and rustc_middle::hooks::Providers::build_mir_inner_impl. The direct pack builder emitted a loadable rouwdi_mir_adapter_probe.wasm, and canonical embedded-registry execution emits real TyCtxt::optimized_mir body identity/hash plus rustc_middle::ty::TyCtxt::collect_and_partition_mono_items output. No fake MIR payload or mono item payload is accepted."
immediate_dependency_blocker = "mir_handoff_payload_adapter.none"
adapter_symbol = "rouwdi_rustc_upstream::mir_handoff_payload_adapter"
adapter_evidence = "The authoritative MIR payload type probe is third_party/rust/src/tools/rouwdi-mir-adapter-probe, registered as an x.py check step and recorded in [[bootstrap_adapter_probes]]. It type-checks the adapter boundary against real rustc_middle MIR/query/session types and the rustc_mir_build provider surface under stage1 bootstrap."

[[components]]
name = "rustc_mir_build"
source_path = "third_party/rust/compiler/rustc_mir_build"
desired_role = "HIR/THIR to MIR lowering and MIR construction for the current handoff"
import_status = "adapter_partially_embedded"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "python x.py check src/tools/rouwdi-mir-adapter-probe --stage 1 -v"
blocker_kind = "none"
exact_blocker = "The bootstrap-owned rouwdi-mir-adapter-probe crate calls rustc_mir_build::provide and then obtains real TyCtxt::optimized_mir output through the embedded-registry loader path. MIR is no longer blocked; the next exact blocker is entering collect_and_partition_mono_items from the real rustc_middle::ty::TyCtxt::collect_and_partition_mono_items output without fabricating mono items."
immediate_dependency_blocker = "mir_handoff_payload_adapter.none"
adapter_symbol = "rouwdi_rustc_upstream::mir_handoff_payload_adapter"
adapter_evidence = "The authoritative bootstrap adapter probe in third_party/rust/src/tools/rouwdi-mir-adapter-probe type-checks rustc_mir_build::provide(&mut rustc_middle::util::Providers) and the MIR build hook surface in the pinned Rust stage1 bootstrap environment."

[[components]]
name = "rustc_monomorphize"
source_path = "third_party/rust/compiler/rustc_monomorphize"
desired_role = "upstream mono item collection and codegen-unit partitioning"
import_status = "adapter_partially_embedded"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "canonical dist/rouwdi.wasm embedded-registry execution of rouwdi_mir_adapter_probe.wasm"
blocker_kind = "none"
exact_blocker = "Canonical embedded-registry execution enters rustc_middle::ty::TyCtxt::collect_and_partition_mono_items through rustc_monomorphize and records mono_items_collected with mono_item_count=1 and mono_item_graph_hash=bec5817d61819666. No fabricated mono items are accepted."
adapter_symbol = "third_party/rust/src/tools/rouwdi-mir-adapter-probe::MonoContactProof::collected"
adapter_evidence = "The embedded payload invokes rustc_monomorphize::partitioning::collect_and_partition_mono_items through TyCtxt::collect_and_partition_mono_items and serializes real MonoItem identities/codegen-unit membership into proofs/monomorphization.json."

[[components]]
name = "rustc_codegen_ssa"
source_path = "third_party/rust/compiler/rustc_codegen_ssa"
desired_role = "shared codegen backend traits, codegen-unit planning, target-machine factory contracts, and object write orchestration"
import_status = "bootstrap_probe_passed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "python x.py check compiler/rustc_codegen_llvm --stage 1 -v"
blocker_kind = "adapter_not_embedded"
exact_blocker = "The pinned Rust bootstrap stage1 check of rustc_codegen_llvm compiled rustc_codegen_ssa as a real upstream dependency, but rouwdi.wasm has not embedded a target-loadable rustc_codegen_ssa/rustc_codegen_llvm backend payload yet."
adapter_symbol = "rustc_codegen_ssa::traits::CodegenBackend"
adapter_evidence = "The successful stage1 rustc_codegen_llvm check compiled rustc_codegen_ssa and reached the upstream CodegenBackend trait surface used by rustc_codegen_llvm::LlvmCodegenBackend."

[[components]]
name = "rustc_target"
source_path = "third_party/rust/compiler/rustc_target"
desired_role = "target specification, ABI, relocation model, and target-machine input data for codegen"
import_status = "bootstrap_probe_passed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "python x.py check compiler/rustc_codegen_llvm --stage 1 -v"
blocker_kind = "adapter_not_embedded"
exact_blocker = "The pinned Rust bootstrap stage1 check of rustc_codegen_llvm compiled rustc_target as part of the real LLVM-grade backend dependency closure, but target-pack codegen has not embedded the target spec into a runnable rouwdi codegen payload yet."
adapter_symbol = "rustc_target::spec"
adapter_evidence = "rustc_codegen_llvm consumes rustc_target for ABI, target feature, and TargetMachine configuration; the stage1 check compiled that dependency through the real upstream crate graph."

[[components]]
name = "rustc_metadata"
source_path = "third_party/rust/compiler/rustc_metadata"
desired_role = "crate metadata and encoded metadata required by codegen and object/module emission"
import_status = "bootstrap_probe_passed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "python x.py check compiler/rustc_codegen_llvm --stage 1 -v"
blocker_kind = "adapter_not_embedded"
exact_blocker = "The pinned Rust bootstrap stage1 check of rustc_codegen_llvm compiled rustc_metadata as part of the real codegen dependency closure, but rouwdi.wasm has not embedded metadata handoff into target-loadable codegen execution yet."
adapter_symbol = "rustc_metadata::EncodedMetadata"
adapter_evidence = "rustc_codegen_llvm imports rustc_metadata::EncodedMetadata; the stage1 check compiled that path through the real upstream backend crate graph."

[[components]]
name = "rustc_llvm"
source_path = "third_party/rust/compiler/rustc_llvm"
desired_role = "LLVM C++ wrapper FFI used by rustc_codegen_llvm for target-machine setup, LLVM module operations, optimization, and object emission"
import_status = "embedded_codegen_payload_executed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "powershell -ExecutionPolicy Bypass -File bootstrap/rustc-codegen-llvm-probe/run-host-probe.ps1; powershell -ExecutionPolicy Bypass -File bootstrap/rustc-codegen-llvm-probe/run-wasm-target-check.ps1"
blocker_kind = "codegen_lowering_to_object_not_implemented"
exact_blocker = "The repo-owned host probe remains evidence for native LLVM contact, and the product route now owns executable wasm32-wasip1 LLVM backend code. The check-only wasm32-wasip1 route exits 0, the executable backend payload links with a wasm32-wasip1 llvm-wrapper archive and target-compatible LLVM library closure, dist/rouwdi.wasm embeds that payload, and the embedded registry executes it to construct rustc_codegen_llvm::LlvmCodegenBackend, create a real LLVM context/module for wasm32-wasip1, initialize WebAssembly target support, create an LLVM target machine, emit real LLVM IR bytes, emit 207 real Wasm object bytes through LLVMTargetMachineEmitToMemoryBuffer(LLVMObjectFile), inspect the object through rouwdi-owned logic, and prove it has no code-bearing mono-item function. Linker handoff remains closed until rustc_codegen_llvm lowers a live mono item through rustc_codegen_ssa::base::codegen_crate; wasm-ld/lld remains the next required assembly-owned payload after that frontier."
adapter_symbol = "rustc_llvm::LLVMRustCreateTargetMachine"
adapter_evidence = "rustc_codegen_llvm's target-machine/object path depends on rustc_llvm LLVMRust* and LLVM C API FFI. The host probe resolves the LLVM C API/C++ library closure through llvm-config and reaches LLVM module and target-machine setup as evidence. The assembly-owned executable payload route now builds the wasm32-wasip1 llvm-wrapper, builds a target-compatible LLVM static closure, links the backend payload through WASI clang/wasm-ld, embeds it in dist/rouwdi.wasm, and executes it from the embedded registry to emit LLVM IR bytes and call LLVMTargetMachineEmitToMemoryBuffer for object emission."

[[components]]
name = "rustc_codegen_llvm"
source_path = "third_party/rust/compiler/rustc_codegen_llvm"
desired_role = "LLVM-grade release codegen backend, LLVM module creation, target-machine setup, and object emission"
import_status = "embedded_codegen_payload_executed"
attempted = true
adapter_path = "crates/rouwdi-rustc-upstream"
probe_command = "python x.py check compiler/rustc_codegen_llvm --stage 1 -v; powershell -ExecutionPolicy Bypass -File bootstrap/rustc-codegen-llvm-probe/run-host-probe.ps1; powershell -ExecutionPolicy Bypass -File bootstrap/rustc-codegen-llvm-probe/run-wasm-target-check.ps1"
blocker_kind = "codegen_lowering_to_object_not_implemented"
exact_blocker = "Real upstream codegen contact is now product-path executable. Host stage1 bootstrap checks rustc_codegen_llvm successfully, and the unsupported wasm32 Enzyme dynamic-library loader path is cfg-gated away. The wasm32-wasip1 check-only route exits 0. The executable wasm32-wasip1 backend payload links with rustc_codegen_llvm, rustc_codegen_ssa, rustc_target, rustc_metadata, rustc_llvm, a wasm32-wasip1 llvm-wrapper archive, and a target-compatible LLVM static closure. dist/rouwdi.wasm embeds and executes that payload from the embedded registry, feeds it MIR and mono proof identity, creates a real LLVM context/module, creates a wasm32-wasip1 target machine, emits LLVM IR bytes, emits 207 real Wasm object bytes through LLVMTargetMachineEmitToMemoryBuffer(LLVMObjectFile), and rouwdi-owned logic reads, hashes, and inspects the object bytes. Object inspection proves the object is empty/probe-only with no mono-item-derived function body, so linker handoff remains closed at codegen_lowering_to_object_not_implemented until rustc_codegen_llvm lowers the live mono item through rustc_codegen_ssa::base::codegen_crate. wasm-ld/lld remains the next required assembly-owned payload after codegen emits a real mono-item Wasm object."
adapter_symbol = "rouwdi_rustc_upstream::rustc_codegen_llvm_backend_probe"
adapter_evidence = "The standalone bootstrap/rustc-codegen-llvm-probe crate owns the direct rustc_codegen_llvm dependency, links a wasm32-wasip1 llvm-wrapper plus target-compatible LLVM library closure, constructs rustc_codegen_llvm::LlvmCodegenBackend through LlvmCodegenBackend::new inside the wasm32-wasip1 payload, emits LLVM IR from the embedded product path, emits Wasm object bytes through LLVMTargetMachineEmitToMemoryBuffer, and exposes the object through rouwdi-owned virtual FS for hash verification. The normal workspace records this evidence without depending directly on rustc_codegen_llvm, because rustc_llvm pins cc=1.2.16 and would otherwise conflict with the runner's wasmtime dependency."

[[frontend_stages]]
stage = "lex"
classification = "upstream_backed_adapter"
current_engine = "rustc_lexer"
upstream_replacement = "rustc_lexer"
ledger_component = "rustc_lexer"
notes = "Lexing already uses the pinned upstream rustc_lexer path through the existing rouwdi-rustc crate."

[[frontend_stages]]
stage = "parse"
classification = "temporary_upstream_adjacent_adapter"
current_engine = "rust-analyzer parser from third_party/rust/src/tools/rust-analyzer/crates/parser"
upstream_replacement = "rustc_parse"
ledger_component = "rustc_parse"
notes = "This stage is scaffolding for syntax ratchet records only and must be replaced by rustc_parse before claiming rustc frontend semantics."

[[frontend_stages]]
stage = "macro_expansion"
classification = "temporary_scaffolding"
current_engine = "rouwdi macro-free detector"
upstream_replacement = "rustc_expand"
ledger_component = "rustc_expand"
notes = "This stage only records that macro expansion is required or not required; it does not expand macros."

[[frontend_stages]]
stage = "name_resolution"
classification = "temporary_scaffolding"
current_engine = "rouwdi token-stream module/import resolver"
upstream_replacement = "rustc_resolve"
ledger_component = "rustc_resolve"
notes = "This local resolver is limited to macro-free compile units and is not canonical rustc name resolution."

[[frontend_stages]]
stage = "type_check"
classification = "temporary_scaffolding"
current_engine = "rouwdi local type checker"
upstream_replacement = "rustc_hir_analysis"
ledger_component = "rustc_hir_analysis"
notes = "This local checker only preserves the frontend ratchet boundary and must not be extended into local Rust semantics."

[[frontend_stages]]
stage = "borrow_check"
classification = "temporary_scaffolding"
current_engine = "rouwdi lexical borrow graph"
upstream_replacement = "rustc_borrowck"
ledger_component = "rustc_borrowck"
notes = "This local checker only preserves the frontend ratchet boundary and must not be extended into local Rust semantics."
